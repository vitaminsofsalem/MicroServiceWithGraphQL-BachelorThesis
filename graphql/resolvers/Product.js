import mongoClient from '../../db.js';

export const Product = {
  category: async (obj, args, context, info) => {
    // console.log(mongoClient('categories'));
    // const category = await mongoClient('categories').findOne({});
    // return category;
  },
};
